import errorHandler from './errorHandler';
import middleware from './middleware';
import routes from './routes';
import Server from './server';

export default { errorHandler, middleware, routes, Server };
